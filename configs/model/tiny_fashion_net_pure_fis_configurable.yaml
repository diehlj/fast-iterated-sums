_target_: src.models.tiny_fashion_net.TinyFashionNetPureFISConfigurable
channels: [1, 4, 8]  # Configurable list of channel sizes (input -> output)
p_drop: 0.0
use_skip_connections: false
skip_connections_gamma: null  # None for no gamma, float for gamma init value
maxplus_linear: false
use_valuation: false
maxplus_normalization: false
semiring: maxplus
use_discounted_sums: false
discount_init: -0.01
use_positional_encoding: false
pe_type: sinusoidal_2d
pe_kwargs: null
pe_before_layer: null  # Which FIS layer to apply PE before (None = before last, matching original)
num_nodes: 2
fis_seeds: null  # null for default [0, 3, 0, 3, ...] pattern, or provide list of length len(channels) - 1
fix_alpha_1: null  # null for all None, or provide list of length len(channels) - 1
sums_internal_activation: null  # None for no activation, 'zero' for debugging, 'relu' for standard activation, ...
pool_mode: max  # "max" or "avg"

